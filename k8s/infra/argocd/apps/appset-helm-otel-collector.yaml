apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: helm-otel-collector
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            region: us-east-1
        values:
          awsRegion: us-east-1
          roleArn: "arn:aws:iam::180294183052:role/production-otel-collector-us-east-1"
          clusterName: multi-region-mall
    - clusters:
        selector:
          matchLabels:
            region: us-west-2
        values:
          awsRegion: us-west-2
          roleArn: "arn:aws:iam::180294183052:role/production-otel-collector-us-west-2"
          clusterName: multi-region-mall
    - clusters:
        selector:
          matchLabels:
            cluster-name: mall-apne2-az-a
        values:
          awsRegion: ap-northeast-2
          roleArn: "arn:aws:iam::180294183052:role/production-otel-collector-ap-northeast-2-az-a"
          clusterName: mall-apne2-az-a
    - clusters:
        selector:
          matchLabels:
            cluster-name: mall-apne2-az-c
        values:
          awsRegion: ap-northeast-2
          roleArn: "arn:aws:iam::180294183052:role/production-otel-collector-ap-northeast-2-az-c"
          clusterName: mall-apne2-az-c
  template:
    metadata:
      name: 'otel-collector-{{name}}'
    spec:
      project: default
      source:
        chart: opentelemetry-collector
        repoURL: https://open-telemetry.github.io/opentelemetry-helm-charts
        targetRevision: 0.115.0
        helm:
          valuesObject:
            fullnameOverride: otel-collector
            image:
              repository: otel/opentelemetry-collector-contrib
            mode: daemonset
            service:
              enabled: true
            tolerations:
              - operator: Exists
            resources:
              requests:
                cpu: 200m
                memory: 768Mi
              limits:
                cpu: "1"
                memory: 2Gi
            serviceAccount:
              annotations:
                eks.amazonaws.com/role-arn: '{{values.roleArn}}'
            presets:
              kubernetesAttributes:
                enabled: true
              logsCollection:
                enabled: true
            config:
              receivers:
                otlp:
                  protocols:
                    grpc:
                      endpoint: 0.0.0.0:4317
                    http:
                      endpoint: 0.0.0.0:4318
              processors:
                batch:
                  send_batch_size: 1024
                  timeout: 5s
                memory_limiter:
                  check_interval: 5s
                  limit_mib: 512
                  spike_limit_mib: 128
                resource:
                  attributes:
                    - key: k8s.cluster.name
                      value: '{{values.clusterName}}'
                      action: upsert
              connectors:
                spanmetrics:
                  histogram:
                    explicit:
                      buckets: [5ms, 10ms, 25ms, 50ms, 100ms, 250ms, 500ms, 1s, 2.5s, 5s, 10s]
                  dimensions:
                    - name: http.method
                    - name: http.status_code
                    - name: service.name
                  exemplars:
                    enabled: true
                  metrics_flush_interval: 15s
              exporters:
                otlp/tempo:
                  endpoint: tempo.observability.svc.cluster.local:4317
                  tls:
                    insecure: true
                awsxray:
                  region: '{{values.awsRegion}}'
                prometheus:
                  endpoint: 0.0.0.0:8889
                  namespace: otel
                  resource_to_telemetry_conversion:
                    enabled: true
                clickhouse:
                  endpoint: tcp://clickhouse-clickhouse.observability.svc.cluster.local:9000
                  database: otel
                  logs_table_name: otel_logs
                  traces_table_name: otel_traces
                  ttl: 720h
                  create_schema: true
                awscloudwatchlogs:
                  log_group_name: /eks/multi-region-mall
                  log_stream_name: otel-collector
                  region: '{{values.awsRegion}}'
              service:
                pipelines:
                  traces:
                    receivers: [otlp]
                    processors: [memory_limiter, batch, resource]
                    exporters: [clickhouse, otlp/tempo, awsxray, spanmetrics]
                  logs:
                    receivers: [filelog]
                    processors: [memory_limiter, batch, resource]
                    exporters: [clickhouse, awscloudwatchlogs]
                  metrics:
                    receivers: [otlp, spanmetrics]
                    processors: [memory_limiter, batch]
                    exporters: [prometheus]
      destination:
        server: '{{server}}'
        namespace: platform
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
