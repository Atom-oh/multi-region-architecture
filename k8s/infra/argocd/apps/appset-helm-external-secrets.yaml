apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: helm-external-secrets
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: region
              operator: Exists
  template:
    metadata:
      name: 'external-secrets-{{metadata.labels.region}}'
    spec:
      project: default
      source:
        chart: external-secrets
        repoURL: https://charts.external-secrets.io
        targetRevision: 0.14.0
        helm:
          valuesObject:
            installCRDs: true
            serviceAccount:
              annotations:
                eks.amazonaws.com/role-arn: arn:aws:iam::180294183052:role/multi-region-mall-external-secrets-{{metadata.labels.region}}
            webhook:
              port: 9443
            certController:
              enabled: true
      destination:
        server: '{{server}}'
        namespace: external-secrets
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
