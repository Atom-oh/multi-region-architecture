apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: prometheus-west-nlb
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            region: us-west-2
  template:
    metadata:
      name: 'prometheus-west-nlb-{{metadata.labels.region}}'
    spec:
      project: default
      source:
        repoURL: git@github.com:Atom-oh/multi-region-architecture.git
        targetRevision: main
        path: k8s/infra/grafana
        kustomize:
          patches:
            - target:
                kind: Service
                name: prometheus-west-nlb
              patch: |-
                - op: add
                  path: /metadata/annotations/service.beta.kubernetes.io~1aws-load-balancer-security-groups
                  value: "sg-0a50d7d48d6e8ed7c"
      destination:
        server: '{{server}}'
        namespace: monitoring
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
