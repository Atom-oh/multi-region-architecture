apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infra-karpenter-crds
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            region: us-east-1
        values:
          karpenterPath: k8s/infra/karpenter
    - clusters:
        selector:
          matchLabels:
            region: us-west-2
        values:
          karpenterPath: k8s/infra/karpenter
    - clusters:
        selector:
          matchLabels:
            cluster-name: mall-apne2-az-a
        values:
          karpenterPath: k8s/infra/karpenter-apne2-az-a
    - clusters:
        selector:
          matchLabels:
            cluster-name: mall-apne2-az-c
        values:
          karpenterPath: k8s/infra/karpenter-apne2-az-c
  template:
    metadata:
      name: 'karpenter-crds-{{name}}'
    spec:
      project: default
      source:
        repoURL: git@github.com:Atom-oh/multi-region-architecture.git
        targetRevision: main
        path: '{{values.karpenterPath}}'
      destination:
        server: '{{server}}'
        namespace: kube-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
