apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: grafana-nlb
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            region: us-east-1
  template:
    metadata:
      name: 'grafana-nlb-{{metadata.labels.region}}'
    spec:
      project: default
      source:
        repoURL: git@github.com:<GITHUB_ORG>/multi-region-architecture.git
        targetRevision: main
        path: k8s/infra/grafana
        kustomize:
          patches:
            - target:
                kind: Service
                name: grafana-nlb
              patch: |-
                - op: add
                  path: /metadata/annotations/service.beta.kubernetes.io~1aws-load-balancer-security-groups
                  value: "sg-0aaaa11111bbbbb22"
      destination:
        server: '{{server}}'
        namespace: monitoring
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
