apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification
  namespace: business-services
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::180294183052:role/production-dsql-access-us-east-1
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  namespace: business-services
  labels:
    app: notification
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification
  template:
    metadata:
      labels:
        app: notification
        version: v1
    spec:
      serviceAccountName: notification
      containers:
        - name: notification
          image: 180294183052.dkr.ecr.us-east-1.amazonaws.com/shopping-mall/notification:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: AWS_REGION
              value: us-east-1
            - name: REGION_ROLE
              value: PRIMARY
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.platform.svc.cluster.local:4317"
            # KAFKA_BROKERS: MSK bootstrap via ExternalSecret
            # SNS_TOPIC_ARN: SNS topic via ExternalSecret
            # SES_CONFIG: SES configuration via ExternalSecret
          envFrom:
            - secretRef:
                name: notification-secrets
                optional: true
            - secretRef:
                name: documentdb-credentials
                optional: true
          resources:
            requests:
              cpu: 125m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health/startup
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: notification
---
apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: business-services
spec:
  selector:
    app: notification
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification
  namespace: business-services
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
