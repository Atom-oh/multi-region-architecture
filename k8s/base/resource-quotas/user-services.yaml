apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: user-services
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 20Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    pods: "50"
